%h1 Repos

%table
  %tr
    %th Pkg
    %th User

  - for repo in @repos
    %tr
      %td= link_to repo.pkg.name, repo
      %td=h repo.username
      %td= link_to 'Edit', edit_repo_path(repo)
      %td= link_to 'Destroy', repo, :confirm => 'Are you sure?', :method => :delete

%br/
= link_to 'New repo', new_repo_path, :rel => "facebox"
