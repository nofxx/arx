<h1>Editing install</h1>

<% form_for(@install) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :pkg %><br />
    <%= f.text_field :pkg %>
  </p>
  <p>
    <%= f.label :code %><br />
    <%= f.check_box :code %>
  </p>
  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>

<%= link_to 'Show', @install %> |
<%= link_to 'Back', installs_path %>
